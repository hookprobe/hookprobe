# POD-002 Logto IAM Configuration
#
# Copy this file to .env and configure for your environment:
#   cp .env.example .env
#   nano .env

# =============================================================================
# Logto Endpoints
# =============================================================================

# Public URL for Logto API (where clients connect)
LOGTO_ENDPOINT=http://localhost:3001

# Admin console URL
LOGTO_ADMIN_ENDPOINT=http://localhost:3002

# =============================================================================
# PostgreSQL Connection (POD-003)
# =============================================================================

# PostgreSQL host (POD-003 IP)
POSTGRES_HOST=10.200.3.12

# PostgreSQL port
POSTGRES_PORT=5432

# Database name for Logto
POSTGRES_DB=logto

# Database user for Logto
POSTGRES_USER=logto

# Database password (REQUIRED - generate a secure password)
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# =============================================================================
# Production Settings
# =============================================================================

# For production, update these:
# LOGTO_ENDPOINT=https://auth.hookprobe.com
# LOGTO_ADMIN_ENDPOINT=https://auth-admin.hookprobe.com
