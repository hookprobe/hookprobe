# HookProbe Fortress - Container Environment Variables
#
# Copy this file to .env and modify the values:
#   cp .env.example .env
#   chmod 600 .env
#
# SECURITY WARNING: Never commit .env to version control!
# The install script generates this file automatically.
#
# This file is read by podman-compose during container startup.
#
# ============================================================
# DATABASE CREDENTIALS
# ============================================================

# PostgreSQL password (auto-generated if not set)
# IMPORTANT: Use a strong random password in production
POSTGRES_PASSWORD=fortress_db_secret

# Redis password (auto-generated if not set)
REDIS_PASSWORD=fortress_redis_secret

# ============================================================
# WEB APPLICATION
# ============================================================

# Flask secret key for session signing (auto-generated if not set)
# Generate with: openssl rand -hex 32
FLASK_SECRET_KEY=fortress_flask_secret_key_change_me

# Web UI port (default: 8443)
WEB_PORT=8443

# ============================================================
# ADMIN CREDENTIALS (used by install script)
# ============================================================

# Admin username (default: admin)
ADMIN_USER=admin

# Admin password (set during installation)
# ADMIN_PASS=your_secure_password_here

# ============================================================
# NETWORK CONFIGURATION
# ============================================================

# Network mode: vlan (OVS port-based segmentation)
NETWORK_MODE=vlan

# LAN subnet mask (23=510 hosts, 24=254 hosts, etc.)
LAN_SUBNET_MASK=24

# ============================================================
# WIFI ACCESS POINT
# ============================================================

# WiFi SSID (network name)
WIFI_SSID=HookProbe-Fortress

# WiFi password (auto-generated if not set)
# WIFI_PASSWORD=your_wifi_password

# ============================================================
# ML/AI SERVICES
# ============================================================

# QSecBit API endpoint (internal)
QSECBIT_API_URL=http://localhost:9090

# dnsXai DNS port (internal)
DNSXAI_PORT=5353

# DFS Intelligence port (internal)
DFS_PORT=8050

# ============================================================
# DNSXAI AI/ML CONFIGURATION (G.N.C. Phase 2)
# ============================================================

# Redis integration for distributed ML caching
# Automatically enabled in container mode (connects to fts-redis)
DNSXAI_REDIS_ENABLED=true
DNSXAI_REDIS_HOST=fts-redis
DNSXAI_REDIS_PORT=6379
DNSXAI_REDIS_DB=1

# ML model paths (use defaults unless custom models)
# DNSXAI_MODEL_PATH=/opt/hookprobe/guardian/models/ad_classifier.pkl
# DNSXAI_LIGHTGBM_PATH=/opt/hookprobe/guardian/models/lightgbm_classifier.txt

# Data and user data directories
DNSXAI_DATA_DIR=/opt/hookprobe/shared/dnsXai/data
DNSXAI_USERDATA_DIR=/etc/hookprobe/persistent/dnsxai

# Protection level (0-5): 0=off, 3=strong (default), 5=maximum
DNSXAI_PROTECTION_LEVEL=3

# ============================================================
# MESH NETWORKING (optional - requires mesh profile)
# ============================================================

# Unique node identifier (auto-generated if not set)
# MESH_NODE_ID=fortress-myname

# Bootstrap peers (comma-separated list of known mesh nodes)
# MESH_BOOTSTRAP_PEERS=mssp.hookprobe.com:8144

# MSSP uplink (for centralized management)
# MSSP_ENDPOINT=https://mssp.hookprobe.com
# MSSP_API_KEY=your_api_key_here

# ============================================================
# REMOTE ACCESS (optional)
# ============================================================

# Cloudflare Tunnel token (for remote access)
# Get from: https://dash.cloudflare.com/
# CF_TUNNEL_TOKEN=your_tunnel_token_here

# ============================================================
# LTE/WWAN FAILOVER (optional)
# ============================================================

# LTE APN (auto-detected if not set)
# LTE_APN=your_carrier_apn

# LTE authentication (NONE, PAP, or CHAP)
# LTE_AUTH_TYPE=NONE

# ============================================================
# DEVELOPMENT/DEBUG
# ============================================================

# Enable debug mode (DO NOT use in production)
# DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
